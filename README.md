# FBB Order Command Center

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy online
1. Create a GitHub repo and upload these files.
2. Go to Streamlit Community Cloud.
3. Deploy `app.py`.
4. Share the generated URL with users.

The app lets anyone upload the source file and use the dashboard + analyzer online.
